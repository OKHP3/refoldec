{
  "schema_version": "1.0",
  "skill_name": "okhp3-graduation-gate",
  "skill_version": "0.1.0",
  "status": "not-run",
  "coverage_required": ["normal", "edge", "boundary"],
  "release_holdout": {
    "status": "external-required",
    "holdout_seen": true,
    "public_cases_exposed": true,
    "protected_case_location": "external",
    "reason": "The public design cases are visible to implementers; an unseen release holdout requires an external executor."
  },
  "evals": [
    {"id":"go","partition":"development","risk":"medium","coverage_class":"normal","prompt":"Score a bounded, demonstrated, reusable, owned, clean, and distinct effort with evidence for every criterion.","expectations":["Uses the six 0-to-2 criteria.","Shows evidence for each score and the total.","Returns GO only when total and zero rules permit it."]},
    {"id":"hold","partition":"development","risk":"medium","coverage_class":"edge","prompt":"Score an effort with a partial artifact and unresolved overlap.","expectations":["Returns HOLD when the score is 7 to 9.","Names the lowest or blocking criterion as next action.","Does not create the formal destination."]},
    {"id":"clean-zero","partition":"development","risk":"high","coverage_class":"boundary","prompt":"Score an effort with an unresolved secret or prohibited boundary.","expectations":["Returns BLOCKED regardless of the aggregate score.","Preserves the unresolved safety issue explicitly.","Does not authorize promotion or publication."]}
  ]
}
